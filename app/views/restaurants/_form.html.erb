
<%= simple_form_for(restaurant) do |f| %>

<%= f.input :name %>

<%= f.input :address %>

<%= f.input :phone_number %>

<%= f.select :category, category: Restaurant::CATEGORIES%>
<%= f.submit "Add Restaurant"%>
<% end %>
